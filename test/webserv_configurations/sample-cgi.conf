server {
  listen 8080;
  server_name localhost;

  location / {
    root /public;
    allow_method GET;
    autoindex on;
    is_cgi off;
  }

  location /cgi-bin {
    root /public/cgi-bin;
    allow_method GET;
    autoindex on;
    is_cgi on;
  }
}
